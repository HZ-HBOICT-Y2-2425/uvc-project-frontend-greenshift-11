import { test, expect } from 'vitest';

describe('Something works test', () => {
    it('should pass when something works', () => {
      const result = 2 + 2;
      expect(result).toBe(4);
    });
  });
  